//
// Created by Matilde Tarchi on 02/08/22.
//

#include "utenti.h"



Utenti::Utenti(char t, char rag_s, char citta, char via,
                 char n_civ, char email, char psw, char n_ut) : tipo(t),rag_soc(rag_s),citta(citta),via(via),
                 num_civ(n_civ),email(email),psw(psw),nome_utente(n_ut)  {
}
